# 01-Proyecto-Zurita

## Cristobal Zurita

A continuación, se detallan los comandos de Git utilizados para la configuración inicial y la subida de los cambios al repositorio:

### Configuración de Git

Muestra una lista de todas las configuraciones globales actuales de Git (como tu nombre y correo, si ya estaban configurados):
```bash

git config --global --list

git config --global user.name komuns

git config --global user.email cristobalkomu@gmail.com

git add .

git commit -m "Readme modificado, nombre agregado"

git push origin main